body {
    width: 100vw;
    color: var(--dark);
    background-color: var(--background-institution);
}

main {
    margin-top: 50px;
}

/* Menu */

#content-header {
    align-items: center;
    height: 140px;
}

.nav-bar {
    grid-column: 2 / 12;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 80px;
}

.nav-bar img {
    width: 50px;
}

/* Especialidades - Listagem */

#return-specialty {
    grid-column: 2 / 12;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

#return-specialty button {
    display: flex;
    align-items: center;
    justify-content: space-evenly;
    color: var(--light);
    background-color: var(--blue-dark);
}

#return-specialty button img {
    width: 37px;
}

/* Especialidades - Conteúdo */

#specialty {
    margin-bottom: 50px;
}

.card-specialty {
    grid-column: 2 / 12;
    margin-top: 50px;
    padding: 25px 8.33vw;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: var(--light);
    border: 3px solid var(--blue);
    border-radius: 40px;
}

.card-specialty .card-info {
    display: flex;
    align-items: center;
    gap: 50px;
}

.card-info .info-professional {
    display: flex;
    align-items: center;
    gap: 40px;
}

.card-info .info-professional img {
    height: 50px;
}

.card-info button {
    color: var(--background-institution);
    background-color: var(--blue);
}